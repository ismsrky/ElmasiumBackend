namespace Mh.Enums
{
    public enum AddressTypes
    {
        xDelivery = -3, // not for UI.
        xFiche = -2, // not for UI.
        xArea = -1, // not for UI.
        xOffice = 0,
        xHome = 1
    }
}